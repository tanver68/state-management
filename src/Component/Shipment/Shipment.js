import React from 'react';

const Shipment = (props) => {
    const {ccc} = props;
    return (
        <div>
            <h2>This is Shipment value : {ccc}</h2>
        </div>
    );
};

export default Shipment;